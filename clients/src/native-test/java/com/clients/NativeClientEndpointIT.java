package com.clients;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeClientEndpointIT extends ClientEndpointTest {

    // Execute the same tests but in native mode.
}